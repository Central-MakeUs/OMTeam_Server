spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  profiles:
    group:
      blue: "prod,blue"
      green: "prod,green"

server:
  env: ${APP_ENV:blue}

